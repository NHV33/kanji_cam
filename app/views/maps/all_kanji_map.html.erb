<%= render 'partials/title_bar', destination: dashboard_url, title: "All User's Collection" %>

<div class="container">
<!-- to be precise, the following is not the exact total kanji number. doesn't consider duplication -->
<p class="text-center mt-4 mb-1"><%= @cards.count %> <%= 'kanji'.pluralize(@cards.count) %> collected on our app</p>
<div class="map mx-auto mb-4"
  data-controller="map"
  data-map-markers-value="<%= @markers.to_json %>"
  data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>"></div>
  <!-- Please advise me good wording... the following will redirect to all user's kanji map -->
<!-- Please advise me good wording... the following will redirect to all user's kanji map -->
<%= link_to "Go back to your chart", maps_path, class: 'btn3d btn-primary text-white mt-4' %>
</div>
